name: cron

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: curl
        run: curl --show-error "https://voice-chain.vercel.app/api/cron/checkTrainer"
